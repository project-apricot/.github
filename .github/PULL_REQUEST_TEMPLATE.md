<!--
  The PR TITLE must be a conventional commit - it becomes the commit message on the
  default branch, which drives the version bump and the release notes. CI enforces it.

    fix: return empty string for whitespace input
    feat!: drop support for the previous major runtime
-->

## What and why

Closes #

## Notes

- [ ] Build is clean and tests pass
- [ ] `docs/` updated, or no behaviour changed
- [ ] Breaking: changes a persistable value, removes public API, or drops a runtime version
